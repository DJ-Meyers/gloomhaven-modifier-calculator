import React, { Fragment } from 'react';
import Title from '../layout/Title';

const Home = () => {
  return (
    <Fragment>
      <Title title={'Rolling Advantage'} />
    </Fragment>
  );
};

export default Home;
